# Node
node_modules/
npm-debug.log*

# Rust / Tauri build output
src-tauri/target/
src-tauri/Cargo.lock.bak

# Tauri-generated bundles
src-tauri/gen/

# OS / editor cruft
.DS_Store
*.swp

# Local env/secrets (API keys are stored in the app's own config dir at
# runtime, never checked into the repo)
.env
.env.*

# Vercel CLI local project link (web_app/) - no secrets in here, but it's
# machine-specific and shouldn't be committed
.vercel
